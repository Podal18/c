/* Задача 1
Определите функцию, которая принимает один параметр типа int,
увеличивает его значение на 5 и выводит его на консоль.*/


#include <stdio.h>

void func_1(int);

int main ()
{
    int n = 8;
    func_1(n);
    return 0;
}

void func_1 (int num)
{
    num = num + 5;
    printf("%d", num);
}



/* Задача 2
Определите функцию, которая принимает один параметр типа int и выводит на консоль его удвоенное значение. 
При этом параметр должен быть константным, чтобы его значение нельзя было изменить в функции.*/

#include <stdio.h>

void func_2(int);

int main ()
{
    int n = 55;
    func_2(n);
    return 0;
}

void func_2 (const int num)
{
    int a = num * 2;
    printf("%d", a);
}


/*Задача 3
Напишите функцию, которая принимает два параметра типа int.
Если первый параметр больше второго, то функция возвращает разность параметров. 
А если первый параметр меньше второго, то функция возвращает сумму параметров.
В функции main вызовите выше определенную функцию несколько раз, передавая ей разные аргументы.*/

#include <stdio.h>

int func (int, int);

int func (int x, int y)
{   
    if (x > y)
    {
        return x - y;
    }
    else if (x < y)
    {
        return x + y;    
    }
    else
    {
        return 0;
    }   
}
 
int main(void)
{
    int num_1 = 1;
    int num_2 = 8;
    int num_3 = 5;
    
    int result = func(num_1, num_2);
    int result_1 = func(num_1, num_3);
    int result_2 = func(num_2, num_3);
    
    printf("%d\n", result);
    printf("%d\n", result_1);
    printf("%d\n", result_2);
}

/* Задача 4
В программе ниже объявите функцию с именем fact, которая вычисляет факториал, 
переданного ей целого числа, 
и возвращает вычисленное значение. Напомню, что факториал любого целого положительного числа n вычисляется по формуле:
n!=1⋅2⋅...⋅n
Если n = 0, то по правилам математики 0! = 1. Для отрицательных чисел функция fact должна возвращать число -1.
Вызовите в функции main функцию fact для вычисления факториала числа 6 и выведите результат в консоль в виде целого числа.*/

#include <stdio.h>

int fact(int n) {
    if (n < 0) {
        return -1;
    } else if (n == 0) {
        return 1;
    } else {
        int result = 1;
        for (int i = 1; i <= n; ++i) {
            result *= i;
        }
        return result;
    }
}

int main() {
    int number = 6;
    int res = fact(number);
    printf("%d", res);

    return 0;
}
